import * as React from 'react';
import MainView from '@components/views/MainView';

export type QuestionPickLayoutProps = {
}

export default class QuestionPickLayout extends React.Component<QuestionPickLayoutProps> {
    render(): React.ReactNode {
        return <MainView>
        </MainView>
    }
}
